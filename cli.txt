# change symlink to another location

$ ln -sfn source_file_or_directory_name softlink_name

# rbenv

# download ruby version
$ rbenv install <version>

# set local rbenv:
$ rbenv local <version>

# install bundler after setting a local rbenv
$ gem install bundler
$ bundel install

# add basic auth
$ htpasswd /home/_httpasswd/.htpasswd tom
$ enter password
